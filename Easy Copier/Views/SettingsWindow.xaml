<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Easy_Copier.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Easy_Copier.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Easy Copier - Settings">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <ToggleSwitch Grid.Row="1" Header="Auto-scan library on startup"
                      IsOn="{x:Bind ViewModel.AutoScanOnStartup, Mode=TwoWay}"
                      Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock Text="Source Folders" FontWeight="SemiBold"/>
            <ListView x:Name="SourceFoldersListView"
                      ItemsSource="{x:Bind ViewModel.SourceFolders, Mode=OneWay}"
                      MaxHeight="300">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind}" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>
                            <Button Grid.Column="1" Content="Remove"
                                    Click="RemoveFolder_Click" Tag="{x:Bind}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <Button Grid.Row="3" Content="Add Source Folder" Command="{x:Bind ViewModel.AddSourceFolderCommand}"
                HorizontalAlignment="Left" Margin="0,16,0,8"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
            <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" VerticalAlignment="Center" Opacity="0.7"/>
            <Button Content="Save and Close" Style="{StaticResource AccentButtonStyle}" Click="SaveAndClose_Click"/>
        </StackPanel>
    </Grid>
</Window>

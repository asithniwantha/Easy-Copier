<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Easy_Copier.Views.OsImageDetailsFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16" MinWidth="280">
        <StackPanel Spacing="12">
            <TextBlock Text="{x:Bind ImageName, Mode=OneWay}"
                       FontWeight="SemiBold"
                       FontSize="16"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="320"/>

            <MenuFlyoutSeparator />

            <Grid RowSpacing="8" ColumnSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Created:" FontWeight="Medium" Opacity="0.8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind DateCreatedFormatted, Mode=OneWay}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Modified:" FontWeight="Medium" Opacity="0.8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind DateModifiedFormatted, Mode=OneWay}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Accessed:" FontWeight="Medium" Opacity="0.8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind DateAccessedFormatted, Mode=OneWay}"/>
            </Grid>

            <TextBlock Text="{x:Bind StatusMessage, Mode=OneWay}"
                       Visibility="{x:Bind IsStatusVisible, Mode=OneWay}"
                       Foreground="Red"
                       FontStyle="Italic"
                       FontSize="12"/>
        </StackPanel>
    </Grid>
</UserControl>
